export type BaseCounterProps = {
  readonly modelValue: number
  readonly minValue?: number
  readonly maxValue?: number
}
export type BaseCounterInputProps = {
  readonly modelValue: number
  readonly maxValue?: number
}
