{/* Typography.mdx */}

import { Meta, Typeset } from "@storybook/blocks";
import { fontSizeData } from "/src/assets/tailwind-config/data/dataFontSize.js";

<Meta title="Typography" />

export const typography = {
  type: {
    primary: "VELUX Transform",
  },
  weight: {
    regular: "400",
    bold: "700",
    extrabold: "800",
    black: "900",
  },
  size: {
    ...Object.keys(fontSizeData).reduce((acc, key, index) => {
      acc[index] = key.toString();
      return acc;
    }, {}),
  },
};

export const SampleText =
  "Lorem ipsum dolor sit amet, consectetur adipiscing elit.";

export const fontSizes = Object.values(typography.size).map((size) =>
  Number(size)
);

# Typography in progress add correct font-weight

**Font:** VELUX Transform

**Weights:** 400(regular), 700(bold), 800(extrabold), 900(black)

<Typeset
  fontSizes={fontSizes}
  fontWeight={typography.weight.black}
  sampleText={SampleText}
  fontFamily={typography.type.primary}
/>
